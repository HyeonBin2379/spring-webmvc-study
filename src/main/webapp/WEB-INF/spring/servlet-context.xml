<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       https://www.springframework.org/schema/context/spring-context.xsd
       http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <!--  경로명이 /WEB-INF/views/로 시작하고 .jsp로 끝나는 뷰를 바인딩 -->
        <property name="prefix" value="/WEB-INF/views/"/>
        <property name="suffix" value=".jsp"/>
    </bean>

    <!--  controller 패키지 내부의 서블릿 객체들을 빈 객체로 등록  -->
    <context:component-scan base-package="com.ssg.springwebmvc.controller"/>
    <!--  resources 폴더의 하위 파일들을 모두 매핑 대상으로 지정하고, 어노테이션 활성화 -->
    <mvc:annotation-driven/>
    <mvc:resources mapping="/resources/**" location="classpath:/static/"/>
</beans>